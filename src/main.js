'use strict';

// Jason- Requirements
const server = require('./lib/server');
// Jason- Allows me to start the server from the root folder
server.start(3000);
//! Jason- Don't know how this works yet but I think I need this here.
// server.close();
